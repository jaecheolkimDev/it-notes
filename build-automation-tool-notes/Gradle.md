Gradle
===========================================


1\. 
----------------
```
./gradlew clean build   : Spring Boot 프로젝트 빌드하기(실행가능한 최종 JAR파일 생성)
./gradlew build -x test

- 주요 옵션:
    - `-x test` : (exclude) test를 제외하고 실행 