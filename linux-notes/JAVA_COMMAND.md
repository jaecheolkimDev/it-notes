리눅스 명령어 공부 note
======================

1\. 리눅스 명령어 정리
-----------------------

1) jps  : Java 프로세스 ID와 함께 적용된 JVM 인자(Arguments)를 모두 보여줍니다.
```
    $ jps -v
```

2) 실행 중인 배치의 실제 JVM 옵션 확인
```
    $ jcmd [PID] VM.flags
```
