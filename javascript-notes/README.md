Javascript
=====================

1\. ECMAScript6
-------------------
1) 변수의 3단계 생명주기 : ①선언 → ②초기화 → ③할당
-------------------
```
- 호이스팅 : js에서 변수와 함수의 선언 부분이 해당 코드 블록(스코프)의 최상단으로 끌어 올려지는 것처럼 보이는 현상.
- TDZ ( Temporal Dead Zone ) : 선언만되고 아직 초기화는 되지 않은 상태

var는 선언과 동시에 undefined로 초기화가 이루어지면서 에러가 발생하지 않는 문제점이 있음.
let,const는 선언은 스코프 최상단으로 끌어올려지지만 초기화는 실제 코드라인에 도달했을때 이루어짐
목적 : 프로그래밍 오류를 줄이는데 있음.
변수는 반드시 선언된 후에 사용해야 한다. 는 규칙을 강제한다.
undefined 라는 애매한 값 대신, ReferenceError 라는 명확한 에러를 발생시켜서 개발자가 실수를 바로 알아차릴 수 있게 도와준다.
```
```
Babel : 6to5 트랜스파일러
코드가 구형 브라우저에서도 돌아갈 수 있게끔 변환해주는 도구.
(최신버전의 자바스크립트가 구버전의 브라우저에서도 실행되도록 변환작업을 해줌.)

ESLint : 자바스크립트 코드 분석기
폴리필 : 이렇게 최신기술을 과거 환경에서 작동하게끔 도와주는 프로그램을 폴리필이라고 합니다.
폴리필 (프로그래밍): 웹 개발에서 기능을 지원하지 않는 웹 브라우저 상의 기능을 구현하는 코드.

임베디드 : 여러 화면에 고정으로 쓰이는 여러 컴포넌트로 이루어진 디비젼.
```
```
ES5 : require , exports
ES6 : import , export
```
```
const 선언은 바인딩을 변경하지 못하도록 하는 것이지, 바인딩 된 값의 변경을 막는 것은 아니다.
```


2\. JS 기반 소스 코드를 실행 파일(.exe 등)로 만들려면 일반적으로 Node.js 환경에서 사용되는 특정 도구를 사용
-------------------
```
JavaScript는 브라우저나 Node.js 같은 런타임 환경에서 인터프리터 방식으로 실행되는 언어이기 때문에,
C++ 같은 컴파일 언어와는 달리 자체적으로 실행 파일을 생성하지는 않습니다. 
JS 소스만으로는 독립 실행 파일을 만들 수 없으며, 
Node.js 기반 도구나 데스크톱 프레임워크를 활용하여 JS 런타임 환경을 포함한 패키지 형태의 실행 파일을 생성해야 합니다.

주요 방법
1. Node.js 애플리케이션의 경우
- Node.js는 주로 서버 측 및 네트워킹 애플리케이션을 구축하는 데 사용되는 런타임 환경
    서버 측 애플리케이션이나 명령줄 도구 등 Node.js 기반 프로젝트는 pkg, nexe, 또는 Bun 같은 도구를 사용하여 단일 실행 파일로 패키징할 수 있습니다. 
    pkg: 프로젝트의 모든 종속성을 분석하고 Node.js 런타임과 함께 단일 실행 파일로 묶어줍니다. 대상 시스템에 Node.js가 설치되어 있지 않아도 실행할 수 있습니다.
    nexe: pkg와 유사한 기능을 제공하며, 조금 더 사용하기 쉽고 빌드 속도가 빠를 수 있습니다.
    Bun: Node.js의 대안으로 떠오르는 런타임으로, --compile 플래그를 사용하여 단일 실행 파일을 생성하는 기능을 지원합니다. 
2. 데스크톱 애플리케이션(GUI)의 경우
- 데스크톱 애플리케이션은 사용자 컴퓨터에 직접 설치되어 독립적으로 실행되는 소프트웨어
    HTML, CSS, JavaScript를 사용하여 데스크톱 애플리케이션을 만들 때는 Electron이나 NW.js 같은 프레임워크를 사용합니다. 이들은 Chromium 웹 엔진과 Node.js 런타임을 포함하는 형태로 앱을 패키징하며, 최종 결과물로 실행 파일을 생성할 수 있습니다. 
    Electron:
    웹 기술로 구축된 애플리케이션을 데스크톱 앱으로 만들어줍니다 (예: VS Code, Slack, Discord). electron-builder 또는 electron-packager 같은 도구를 사용하여 최종 배포용 실행 파일을 생성합니다. 
```


3\. 화살표 함수 : 결과를 자동으로 반환(Implicit Return)하는 규칙
-------------------
```
1) 중괄호를 사용하면 여러 줄짜리 코드 블록이라 return을 직접해줘야함.
```


4\. 특징
-------------------
```
1) 싱글 스레드 기반의 동기적 언어이다.
2) 브라우저나 Node.js 같은 환경의 도움을 받아 비동기 처리를 효율적으로 수행한다.(콜백, 프로미스, async-await 등)
 - 응답을 기다리지 않고 다음 코드로 넘어가는 비동기 처리를 사용한다.
```


5\. JavaScript 기반 프레임워크
-------------------
```
명칭	        분류	        기반 언어	핵심 특징
JavaScript	프로그래밍 언어	-	    모든 것의 기본 (알파벳/문법)
Node.js	    실행 환경	JavaScript	JS를 컴퓨터에서 실행하게 해줌
Express.js	프레임워크	JavaScript	서버 기능을 쉽게 쓰게 해줌 (서버 방언)
Nest.js     프레임워크    JavaScript  서버 기능을 쉽게 쓰게 해줌 (서버 방언)
Vue.js	    라이브러리	JavaScript	화면을 쉽게 그리게 해줌 (화면 방언)
React	    라이브러리	JavaScript	화면을 쉽게 그리게 해줌 (화면 방언)
Next.js	    프레임워크	JavaScript	React 기반의 종합 관리 도구

가볍고 빠르게: Vue.js (화면) + Express.js (서버)
가장 대중적으로: React (화면) + Express.js (서버)
대규모 기업용으로: React/Vue (화면) + Nest.js (서버)
검색 노출이 중요한 웹: Next.js (전체)
```