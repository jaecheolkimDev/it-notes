vue.js 공부 note
======================

0\. 반응형
-----------------------
```
반응형이란, 데이터가 변경되었을 때 이를 감지하고 이에 반응하여 DOM이 자동으로 업데이트 되는 성질을 말한다. 즉, 데이터를 변경하면 화면이 변경되는 것이다.
```

1\. 라이브러리
-----------------------
1) 라이브러리
```

```

2\. 가상 스크롤(Virtual Scroll)
-----------------------
```
화면에 보이는 요소만 렌더링함으로써 대량의 데이터를 다룰 때 성능을 개선합니다.
```

3\.
-----------------------
```
extends     : 기존 컴포넌트를 확장해서 유사한 컴포넌트를 만들 수 있음.
```

4\. v-html
-----------------------
원리나 기능은 웹 표준 기술인 innerHTML에 기반하고 있습니다.
```
v-html은 전달받은 문자열을 **"이건 안전한 HTML이니까 브라우저야 그대로 렌더링해줘"**라고 명령하는 것입니다.
```

5\. 라이프사이클
-----------------------
```
* 팝업은 v-if로 컴포넌트를 파괴하고 생성하는 작업을 해줘야지 동작한다.

1) created
    data 속성과 methods 속성이 정의 되었기 때문에 this.data or this.fetchData()와 같은 로직들을 이용해
    data 속성과 methods 속성에 정의된 값에 접근해 로직을 실행할 수 있다.
    아직 인스턴스가 화면에 부착되기 전이기 때문에 template 속성에 정의된 돔 요소로 접근할 수 없다.
    data속성과 methods 속성에 접근할 수 있는 첫 라이프 사이클 단계이며,
    컴포넌트가 생성되고 나서 실행되는 단계이기 때문에 서버에 데이터를 요청하여 받아오는 로직을 수행하기에 좋다.

2) mounted
    el 속성에서 지정한 화면 요소(DOM)에 인스턴스가 부착되고 나면 호출되는 단계로,
    template 속성에 정의한 화면 요소에 접근할 수 있어 화면 요소를 제어하는 로직을 수행하기 좋은 단계이다.
    돔에 인스턴스가 부착되자마자 바로 호출되기 때문에 하위 컴포넌트나 외부 라이브러리에 의해
    추가된 화면 요소들이 최종 HTML 코드로 변환되는 시점과 다를 수 있다.
    
3) beforeDestroy
    컴포넌트가 닫히기전에 하는 동작
```

6\. 팝업
-----------------------
```
팝업을 v-show로 사용할때는 Vue의 비동기 렌더링 방식 때문에 v-show에 들어가는 변수값을 세팅한 후 nextTick을 통해서 팝업을 호출해야 한다.
변수를 바꾸자마자 $refs로 호출하면 Vue는 아직 팝업을 그리는 중이라 undefined 에러를 뱉게 됩니다.
```

7\. props
-----------------------
```
상하위 컴포넌트 관계
컴포넌트는 각각 고유한 유효 범위를 갖고 있기 때문에 직접 다른 컴포넌트의 값을 참조할 수 없다.
뷰 프레임워크 자체에서 정의한 컴포넌트 데이터 전달 방법을 따라하며,
가장 기본적인 전달 방법은 상위(부모) - 하위(자식) 컴포넌트간의 데이터 전달 방법이다.
부모에서 자식으로는 props 속성을 전달할 수 있다. 자식에서 부모로는 기본적인 이벤트만 전달할 수 있다.
* props에서 받을 파라미터의 타입을 정해놓으면 부모에서는 일치하는 타입만 보내야 한다.
```


999\. 
-----------------------
```
```