실시간 통신 기술
=================

7\. 실시간 통신 기술 비교
-----------
```
구분              Polling             WebSocket                       SSE (Server-Sent Events)
통신 방향           단방향 (요청-응답)     양방향                         단방향 (서버 ➔ 클라이언트)
프로토콜            HTTP                WS / WSS (별도 프로토콜)          HTTP
재연결 기능          없음 (매번 새 요청)    직접 구현 필요                자동 재연결 기본 지원
복잡도             단순함                 높음                          낮음 (구현이 쉬움)
주요 유스케이스        단순 주기적 조회   "실시간 채팅, 멀티플레이 게임"      "LLM 답변 스트리밍, 실시간 알림, 주식 시세"
```

4\.
-----------
```
```

5\.
-----------
```
```

6\.
-----------
```
```

7\.
-----------
```
```