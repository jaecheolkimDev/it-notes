Podman(Pod Manager)
================

1\. 개념
-----------------------------
```
레드햇(Red Hat)이 개발한 오픈소스 컨테이너 엔진으로, 도커(Docker)를 대체할 수 있는 강력하고 안전한 도구입니다. 도커와 동일한 수준의 명령어 호환성을 
제공하지만, 백그라운드 데몬(Daemon)이 없고 루트 권한 없이(Rootless) 컨테이너를 실행할 수 있는 것이 핵심 특징입니다.
```