데코레이터 패턴(기능의 확장)
=====================
데코레이터 패턴은 기능의 확장에 초점을 맞춘 패턴입니다.
```
비즈니스 로직은 그대로 두고, 추가적으로 새로운 기능(트랜잭션 시작/종료)을 덧붙입니다.

트랜잭션에서의 적용 :
    비즈니스 로직을 수행하는 Service를 데코레이터로 감싸서, 로직 전후에 begin()과 commit()/rollback()이라는 기능을 추가합니다.
    사용자(Client)는 여전히 같은 인터페이스를 보지만, 실제로는 강화된 기능을 실행하게 됩니다.
```