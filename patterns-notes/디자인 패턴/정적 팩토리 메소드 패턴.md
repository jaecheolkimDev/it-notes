정적 팩토리 메소드 패턴
=================

1\. 개념 : 클래스 내부에 public static 메소드를 만들어 객체 생성을 대신하게 하는 방식입니다.
-----------
```
이름을 가질 수 있음: new Class()와 달리 from(), of(), getInstance() 등 목적에 맞는 이름을 붙여 가독성을 높입니다.

생성 제어: 매번 새로운 객체를 만들지 않고 캐싱된 객체를 반환하거나(Singleton), 하위 타입 객체를 반환할 수 있습니다.
```